<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.education.supplier.dao.SubjectDao">
    <select id="listSubject" resultType="com.education.supplier.util.model.One2Many">
        select id,
            name
        from t_subject
        order by id
    </select>
</mapper>